import ScrollAppear from 'scroll-appear';

const SocialShare = {

    init() {

        new ScrollAppear(".is-visible", {
            defaultToggleClass: "is-not-visible"
        });

    }
}

export default SocialShare;